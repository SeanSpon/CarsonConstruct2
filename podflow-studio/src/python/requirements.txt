# Core audio processing
librosa>=0.10.1
# Numba (a dependency of librosa) is not yet compatible with NumPy 2.4+
numpy>=1.24.3,<2.4
scipy>=1.11.3
soundfile>=0.12.1
# WebRTC VAD (optional): if not installed, the app falls back to an energy-based VAD.
# On Windows/Python versions without a compiler toolchain, installing this may require
# Microsoft C++ Build Tools.
# webrtcvad
pydub>=0.25.0

# Speaker diarization (for multi-camera switching)
# Note: pyannote.audio requires torch and HuggingFace token
# pip install pyannote.audio torch
pyannote.audio>=3.0.0
torch>=2.0.0

# Environment
python-dotenv>=1.0.0

# Face detection for auto-orient
opencv-python>=4.8.0
opencv-python-headless>=4.8.0

# AI Providers (install the ones you need)
# ---------------------------------------
# OpenAI - GPT-4o, GPT-4o-mini, Whisper
openai>=1.12.0

# Google Gemini - 1.5 Pro, 1.5 Flash (free tier available)
# pip install google-generativeai
google-generativeai>=0.4.0

# Anthropic Claude - 3.5 Sonnet, Opus, Haiku
# pip install anthropic
anthropic>=0.18.0

# Local AI (Ollama)
# pip install ollama
ollama>=0.1.6

# Local Transcription (optional)
# Provides word-level timestamps for karaoke-style captions.
# NOTE: On some Windows/Python versions, faster-whisper may require a Rust toolchain
# because tokenizers wheels may not be available.
# Install only if you want local transcription:
#   pip install "faster-whisper>=0.10.0"
